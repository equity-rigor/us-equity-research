# Phase B0 / D23 — third-party plugin source, reproducible via /plugin marketplace add
reference/anthropic-official/

# Standard Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.venv/
venv/
.pytest_cache/

# Excel / Office artifacts
~$*.xlsx
~$*.docx
~$*.pptx
*.tmp

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Outputs (per memo are stored under outputs/ at the project root or workspace)
# Phase E NVDA self-test outputs are committed; ad-hoc runs are not.
# outputs/*.md  -- intentionally NOT ignored; we commit Phase E deliverables
# outputs/*.json -- same

# Personal application materials — never commit
point72-*
*case-study*
*case_study*
~/Documents/point72-app/

# Internal author design history — local-only, not part of the public framework
design/

# Construction-phase prompt — local archive only; contradicts portfolio-piece framing
BUILD_PROMPT.md

# Entitlement-restricted research — NEVER commit (G23 source-tier public boundary).
# Client-only sell-side notes / NDA expert calls / subscription feeds used only as
# personal, non-persisted, localized cross-checks. The framework is purely-public-info.
# Root-level PDFs are where broker notes land; our public deliverable PDFs live in outputs/.
/*.pdf
/*.docx
research-notes/
